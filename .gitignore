# Claude Code local settings (per-user, not shared)
.claude/settings.local.json

# Helm
charts/
**/charts/*.tgz
Chart.lock

# Kubernetes / Argo CD local state - never commit real credentials or kubeconfigs
*.secret.yaml
secrets/
.kube/
kubeconfig*
*.key
*.crt

# OS
.DS_Store
