<!-- AI-AGENT NOTE: leaf-directory CLAUDE.md, scoped to this directory. Root
CLAUDE.md has the whole-repo index. -->

# docs/

Reader-facing documentation for this repo (as opposed to `specs/`, which is
Spec Kit's own planning/process trail).

## What's here

- `PREREQUISITES.md` — resolved local tool versions and the "Pinned Chart
  Versions" table (the source of truth for upstream dependency versions —
  don't duplicate it elsewhere)
- `runbooks/bump-chart-version.md` — 3-step version-bump procedure
- `pattern/` — the actual pattern documentation: architecture, the library
  reuse trick, a reproduction guide, a pitfalls register, and an
  applicability assessment guide. **Start at
  [`pattern/README.md`](./pattern/README.md)**, not by reading files in
  this directory directly.

## Read next

- `pattern/README.md` — entry point for everything else
